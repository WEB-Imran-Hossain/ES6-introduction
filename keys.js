// using object for keys
const glass = { 
    name: 'glass', 
    color: 'golden', 
    price: 12, 
    isCleaned: true 
};

// console.log(glass);

// all properties name
const keys = Object.keys(glass);
console.log(keys);